---
title: 121 买卖股票的最佳时机
url: https://www.yuque.com/wcywxq/gklbh2/eqag967oiizb6p3i
---

[121. 买卖股票的最佳时机](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock)

```javascript
/**
 * 动态规划
 * @param {number[]} prices
 * @return {number}
 */
var maxProfit = function(prices) {
  // dp[i]: 截止到第 i 天，最低的买入价格
  // dp[i] = min(dp[i - 1], nums[i])
  let maxProfit = 0
  let dp = new Array(prices.length).fill(Number.MAX_VALUE)
  dp[0] = prices[0]
  for (let i = 1; i < prices.length; i++) {
    dp[i] = Math.min(dp[i - 1], prices[i])
    maxProfit = Math.max(maxProfit, prices[i] - dp[i])
  }
  return maxProfit
}

/**
 * 动态规划 - 节省空间
 * @param {number[]} prices
 * @return {number}
 */
var maxProfit = function(prices) {
  let maxProfit = 0
  let minBuyPrice = prices[0]
  for (let i = 1; i < prices.length; i++) {
    minBuyPrice = Math.min(minBuyPrice, prices[i])
    maxProfit = Math.max(maxProfit, prices[i] - minBuyPrice)
  }
  return maxProfit
}
```
