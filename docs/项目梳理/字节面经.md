---
title: 字节面经
url: https://www.yuque.com/wcywxq/aggvdq/gq8kd3
---

```javascript
// 获取函数参数类型
function add(a: string, b: number, c: boolean, d: [string, number], e: symbol, f: undefined) {
  console.log(arguments)
}

type Params<T> = T extends (...args: infer P) => void ? P : never

type GetAddParams = Params<typeof add>

let getAddParams: GetAddParams = ['2', 1, false, ['1', 3], Symbol('1'), undefined]

// ts 中 extends 关键字的作用
// jwt 的工作模式
// cookie 的相关字段
// cookie 泄漏问题
// 为什么 javascript 事件循环需要将异步任务分为宏任务和微任务
// nodejs 事件循环
// http 缓存
// cache-control 中 no-cache 和 no-store 的区别是什么
// cache-control 的最小时间
// node 事件循环，为什么 node 的事件循环需要设计 6 个阶段
// 对称加密和非对称加密的区别
// https 使用了哪种加密方式

// 实现数组的补集
function complement(arr1: number[], arr2: number[]) {
  return [
    ...arr1.filter(val => arr2.indexOf(val) === -1),
    ...arr2.filter(val => arr1.indexOf(val) === -1)
  ]
}
console.log(complement([3, 2, 5, 8], [5, 3, 11]))
```

```javascript
// 介绍一下 cookie
// js 事件循环机制、node 事件循环机制
// let 和 const 存在变量提升吗？创建时被提升，然而初始化时并没有被提升
// promise.all 和 promise.race 的区别
// promise.all 的实现
// LRU 缓存机制
// http 和 https 的区别和介绍
// tcp 的三次握手和四次挥手

// vue3 和 vue2 的区别
// vue 的数据响应式原理
// composition-api 和 react hooks 的区别

// css border 可以做什么
// position: sticky 相关问题

/**
 * typescript 的优点
 */
// 1. 更好的文档支持：类型提供了一种描述对象形状的方法，可以帮助提供更好的文档，还可以让 typescript 验证你的代码可以正常工作
// 2. 静态类型检查：静态类型检查可以避免很多不必要类型的错误，在编译阶段提前发现问题
// 3. 强大的类型推断能力：除了类型声明外，typescript 提供了强大的类型推断能力，该能力能够大大减少我们需要编写的类型声明，有效地减少我们 typescript 的额外压力
// 4. ide 智能提示：目前主流的 ide 都对 typescript 提供了良好的支持，基于 typescript 的类型系统提供友好准确的代码提示与错误检查

// 在项目中是如何重构 ts 的
// ts 中泛型解决了什么问题
// ts 中 unknown 和 any 的区别
// ts 中的 Omit 和 keyof
// ts 通用类型保护函数 isOfType 实现
interface interfaceA {
  name: string
  age: number
}
interface interfaceB {
  name: string
  phone: number
}
const obj1 = { name: 'zs', age: 18 }
const obj2 = { name: 'ls', phone: 1311234567 }
const arr1 = new Array<interfaceA>(50).fill(obj1)
const arr2 = new Array<interfaceB>(50).fill(obj2)
const target = [...arr1, ...arr2][0]
function isOfType<T>(varToBeChecked: unknown, propertyToCheckFor: keyof T): varToBeChecked is T {
  return (varToBeChecked as T)[propertyToCheckFor] !== undefined
}
if (isOfType<interfaceA>(target, 'age')) {
  console.log(target.age)
} else {
  console.log(target.phone)
}

// webpack5 和 webpack4 的区别
// webpack 构建速度优化
// webpack 插件中常用的一些钩子
// @babel/core 是如何进行 ast 变换的
// @babel/core 的工作原理
// webpack 中 loader 的执行顺序
```
